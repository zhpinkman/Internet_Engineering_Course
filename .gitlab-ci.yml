# This file is a template, and might need editing before it works on your project.
docker-build-master:
  # Official docker image.
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u "$DOCKER_HUB_REGISTRY_USER" -p "$DOCKER_HUB_REGISTRY_PASSWORD" $DOCKER_HUB_REGISTRY
  script:
    - docker build --pull -t java-server ./CA_08_mzFoodDelivery/server/CA_07_mzFoodDelivery/Dockerfile
    - docker push java-server
    - docker build --pull -t react-app ./CA_08_mzFoodDelivery/react_app/Dockerfile
    - docker push react-app
  only:
    - master